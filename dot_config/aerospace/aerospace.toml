start-at-login = true

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

default-root-container-layout = 'tiles'

accordion-padding = 0

default-root-container-orientation = 'auto'

indent-for-nested-containers-with-the-same-orientation = 30

gaps.inner.vertical = 15
gaps.inner.horizontal = 15
gaps.outer.left = 10
gaps.outer.bottom = 5
gaps.outer.top = 5
gaps.outer.right = 10

[mode.main.binding]
alt-m = 'workspace main'
alt-s = 'workspace secondary'
alt-p = 'workspace programming'

alt-shift-m = 'move-node-to-workspace main'
alt-shift-s = 'move-node-to-workspace secondary'
alt-shift-p = 'move-node-to-workspace programming'

alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'

alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

alt-f = 'fullscreen'

alt-shift-semicolon = 'mode service'

[mode.service.binding]
r = ['flatten-workspace-tree', 'mode main']
esc = ['reload-config', 'mode main']

[workspace-to-monitor-force-assignment]
main = 'main'
secondary = 'secondary'
programming = 'main'
